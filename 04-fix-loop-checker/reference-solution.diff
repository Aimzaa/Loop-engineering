diff --git a/src/paginate.js b/src/paginate.js
index b43101d..0897282 100644
--- a/src/paginate.js
+++ b/src/paginate.js
@@ -5,7 +5,7 @@
  * Example: paginate([1..10], 1, 3) -> [1, 2, 3]
  */
 function paginate(items, page, perPage) {
-  const start = page * perPage;
+  const start = (page - 1) * perPage;
   return items.slice(start, start + perPage);
 }
 
@@ -14,7 +14,7 @@ function paginate(items, page, perPage) {
  * Example: pageCount(10, 3) -> 4
  */
 function pageCount(count, perPage) {
-  return Math.floor(count / perPage);
+  return Math.ceil(count / perPage);
 }
 
 module.exports = { paginate, pageCount };
