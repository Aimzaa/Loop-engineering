[would POST to Slack #eng-standup]

# Daily Triage - 2026-09-03
_project: cli/cli  (stand-in for YOUR repo) | window: 24h | channel: #eng-standup_

@here - P0 items need eyes now.

## P0 (1)
- CI "Bump Go" (trunk) | Bump Go | main-branch CI is red | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch

## P1 (9)
- #90002 | gh auth login ignores --hostname on Windows | confirmed bug
    -> confirm repro, then size it
- #14323 | gh auth refresh hangs forever on the (Y/n) prompt when stdou | unowned, needs a look | CARRIED from a prior day
    -> assign an owner in standup
- #14322 | auth token --user can return stale keyring token after --ins | confirmed bug | CARRIED from a prior day
    -> confirm repro, then size it
- #14317 | `gh pr edit --title` fails when a team reviewer is inaccessi | unowned, needs a look | CARRIED from a prior day
    -> assign an owner in standup
- CI "Lint" (williammartin-api-host-commit-split) | Lint | a CI failure on some branch | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch
- CI "Code Scanning" (williammartin-api-host-commit-split) | Code Scanning | a CI failure on some branch | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch
- CI "Unit and Integration Tests" (williammartin-api-host-commit-split) | Unit and Integration Tests | a CI failure on some branch | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch
- CI "Unit and Integration Tests" (docs/14075-claude-import) | Unit and Integration Tests | a CI failure on some branch | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch
- CI "Lint" (fix/13804-gh-issue-create-errors-after-successful) | Lint | a CI failure on some branch | CARRIED from a prior day
    -> re-run once; if still red, page the last committer on that branch

## P2 (7)
- #14309 | `--attach` cannot work for GitHub App installation tokens —  | nice-to-have
    -> confirm repro, then size it
- #14090 | [aw] Detection Runs | uncategorised - default bucket
    -> confirm repro, then size it
- #13872 | gh auth status should indicate when a token is stored in pla | nice-to-have
    -> confirm repro, then size it
- #14075 | Claude Code doesn't read AGENTS.md, so our agent instruction | uncategorised - default bucket
    -> confirm repro, then size it
- #14302 | `--attach` requires write access, so outside reporters canno | nice-to-have
    -> confirm repro, then size it
- #14308 | Support for Archiving Labels | nice-to-have
    -> confirm repro, then size it
- #10635 | can `gh gist list` support json formatted output? | nice-to-have
    -> confirm repro, then size it

## Biggest risk today
- CI "Bump Go" (trunk) Bump Go - highest P0, act first.

_ignored this run: 0 (wontfix, dependabot[bot])_
